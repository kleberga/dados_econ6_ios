workflows:
  default:
    name: Default Workflow
    environment:
      groups:
        - default
      vars:
        BUNDLE_ID: "com.dadoseconomicos6.app"
    scripts:
      - name: Build Flutter app
        script: |
            keychain initialize
            keychain add-certificates
            xcode-project use-profiles
            flutter build ios --release --no-codesign
            xcode-project build-ipa --workspace ios/Runner.xcworkspace --scheme Runner --archive-flags "-allowProvisioningUpdates -configuration Release"

